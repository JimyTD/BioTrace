# BioTrace Web 环境变量示例
# 复制为 .env.local（本地开发）或在构建机上导出后再build。
#
# 注意：Vite 的 VITE_* 变量是**构建时内联**进产物的，不是运行时读取。
# 生产部署必须在执行 `pnpm --filter @biotrace/web build` 之前设置好，
# 移动端 `pnpm mobile:sync` 同理（同样走 web 构建产物）。

# 天地图「浏览器端」主 key。留空则直接用内置简图（国界 / China POV，无国名注记）。
# 申请：https://passport.tianditu.gov.cn/ → https://lbs.tianditu.gov.cn/ 控制台 → 创建应用（浏览器端）
# 白名单需包含开发地址（本项目 dev server 为 127.0.0.1:5173）与生产域名。
# 后端逆地理编码需要另一个「服务端」key，不在此文件。
VITE_TIANDITU_KEY=

# 可选：浏览器端备用 key（同账号再开浏览器端应用即可）。
# 主 key 失败 → FALLBACK → FALLBACK_2 → 内置简图。
# FALLBACK 也可写成「keyA,keyB」一把变量塞两个；不要写服务端 key。
VITE_TIANDITU_KEY_FALLBACK=
VITE_TIANDITU_KEY_FALLBACK_2=
