---
description: Web 皮肤与流程页分离；加功能必须过皮肤清单，禁止在页面写死品牌色
alwaysApply: true
---

# Web 皮肤主题（强制）

表现层五分离：**文案** `packages/messages` · **配色** `apps/web/src/themes/` · **结构摆放** `styles.css` · **整块换实现** `themes/slots.ts` · **流程与数据** `pages/`。

细则与 token 表见 [`docs/features/皮肤主题.md`](docs/features/皮肤主题.md)。当前皮肤：默认 `clear`（清透，无世界观，只有观感），备选 `daylight`（日光图版）。账号皮肤存在 `users.theme`；`applyTheme` 仍写本机缓存作首屏。`/admin`、地图在线瓦片不换皮；引导插页尚未主题化。

## 加功能时过一遍

命中就补；判断为不用补的，汇报里写一句为什么对**所有已登记皮肤**都一样。禁止默默写死。

1. 新的用户可见中文 → `packages/messages`，用 `t()`。某套皮肤要换说法再加 `voices/`，不改状态码、不改含义。
2. 新的颜色 / 字体 / 圆角 / 阴影 → `var(--*)`。没有合适 token 就新开，**同轮给所有已登记皮肤赋值**。漏填是 `unset`，不是回退。
3. 新的承载质感的结构件 → 零件一次渲全，默认藏着，各皮肤用 `[data-theme]` 决定摆哪、亮哪。不要用条件渲染决定零件在不在。
4. 新的主题化图片 → `public/<域>/<themeId>/`，只经 `themes/*Assets` helper。不要写死某个皮肤的路径。
5. 新的动作，或新的「概念的表现方式」（等级、仪式、状态章）→ 先判断要不要开 `themes/slots.ts` 槽位。换的是物件或演法，不是颜色，才开槽。

切换皮肤用 `applyTheme`（本机缓存）。登录后以账号 `users.theme` 为准，外观页写入账号。新皮肤：CSS → 资源包 → 登记 `ThemeId` / `THEME_IDS` / `THEME_META` → API `theme-id.ts` 一并登记 → `main.tsx` import → `theme.<id>` / `theme.<id>Hint`。

## 禁止

- 在 `pages/*` / `components/*` 写死主题十六进制色或某个皮肤的资源路径
- 皮肤触碰数据、状态机、请求、路由、埋点
- 只改某一皮肤、却在 `styles.css` 留下硬编码品牌色
- 未在 `THEME_IDS` 登记的 id 当作可用皮肤
- 声明没有人读的资源域，或声明没人调用的槽位
- 借换皮改内部状态码或增删功能——换摆法和说法，不换含义
- 为换皮复制 `pages/*` 或分叉业务流程

## 验收

- 有风格示意页的，对着示意看，不只确认「皮肤生效了」
- 只搬结构、不改观感时，默认皮肤不能漂
- 改结构样式时别写死浅色前提
