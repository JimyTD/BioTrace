---
description: Web 皮肤主题与流程页分离；加主题按 docs/06 §10，禁止在页面写死品牌色
alwaysApply: true
---

# Web 皮肤主题（强制）

表现层三分离：**文案** `packages/messages` · **皮肤** `apps/web/src/themes/` · **流程 UI** `pages/` + `styles.css`。

完整清单与 token 表见 [`docs/06-实现与功能规格.md`](docs/06-实现与功能规格.md) §10。

## 必须

- 配色 / 字体 / 圆角 / 氛围底 / 稀有度徽章色：写在 `apps/web/src/themes/<id>.css` 的 `[data-theme="<id>"]` 下
- 结构样式只使用语义 `var(--*)`（见 `apps/web/src/styles.css`）
- 新增皮肤：加 CSS → 登记 `themes/index.ts` 的 `ThemeId` / `THEME_IDS` → `main.tsx` import →（可选）`applyTheme` 切换 UI
- 切换皮肤用 `applyTheme`；不要复制整页组件换皮

## 禁止

- 在 `pages/*` / `components/*` 里写死主题十六进制色或为换皮分叉业务流程
- 只改某一皮肤、却在 `styles.css` 留下硬编码品牌色
- 未在 `THEME_IDS` 登记的 id 当作可用皮肤

## 当前与后置

- 默认：`daylight`（日光胶片 · 旅游）
- 后置示例：`tide`（潜水/潮间带）— 按 §10.3 清单加，勿改核心流程
