---
description: Git 提交说明必须使用中文
alwaysApply: true
---

# 提交说明（强制中文）

为本仓库创建 git commit 时，**提交说明（commit message）必须使用简体中文**。

## 必须

- 标题与正文用中文写清「为什么改 / 改了什么」
- 可保留惯用前缀：`feat:` / `fix:` / `docs:` / `chore:` 等，后接中文简述
- 多条改动时用中文分点，勿整段英文复述

## 示例

```text
feat: 相册支持批量上传与缩略图预览

多选时隐藏单张说明；串行上传并显示进度。
```

## 禁止

- 默认用英文写 commit message（专有名词、API 路径、代码标识除外）
- 空洞说明（如仅写 `update` / `fix` / `更改`）
