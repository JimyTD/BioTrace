# 文案改造 · 进度清单

> 逐模块维护。**条数一律实算**（`python scripts/.tmp-count.py`，正则可识别多段 key），**状态以 `git log` 为准**。
> 状态口径：**已改**（本轮动过并提交）· **已审·无改动**（过完但一条不改）· **待审** · **跳过**（用户明示）。

实算合计 **730 条 / 24 个模块**（`zhFlavor` 可文案区 34，`zhCore` 固定区 696）。

## 本次改造原则（2026-09-11 用户补）

- **判断措辞时不引入分区。** 逐模块改造只做精简（去句号、删废话、合并重复、改措辞），分区归属不参与措辞判断、也不在提案里提。
  （分区另有独立一条线：2026-09-14 起用户主动讨论主题化与分区，不再禁止。）
- **准则不是越多越好（2026-09-14 用户洞察）。** 旧准则多是踩坑累积，堆得越多，agent 越会「处处遵守」，写出来越蹩脚——同一个需求，约束更少的模型反而写得更好。本次要**逐条精简**，不是继续加。
- **三层文案（2026-09-14 用户定）**：基础表（常规，保守兜底，不需要好看、只需不会读错）+ 每套皮肤自己的说法（voice 覆盖表）。基础表不替主题做决定——例：「证书」从基础表退出，收进主题层。
- **默认不入主题（2026-09-14 用户定）**：分区是**准入许可，不是待办清单**。没想清楚该不该被主题覆盖的，默认留固定区；想清楚要主题化了再挪进可文案区。细节见《文案工程规范》§三。

| # | 模块 | 条数 | 状态 | 提交 | 备注 |
|---|------|------|------|------|------|
| 1 | admin | 323 | 跳过 | — | 用户明示本轮及后续跳过（后台术语） |
| 2 | me | 40 | 已改 | `f95e1eb` `dd5a2f4` | 删 `changingPassword`/`appVersionUnknown`/`appearanceLede`；`identifyQuotaHint`/`identifyKeyLede`/`changePasswordAction`/`updateMissing` 改措辞 |
| 3 | detail | 34 | 已改 | `70572f7` | `acceptedScientificName` 补名词；删 `softReasonFallback`、`keepsakeReasonFallback` 两条上游必填兜底 |
| 4 | trips | 32 | 已改 | `8919ba9` `8106469` `2516313` | 旅途信息一节定稿为三句名词短语（用户给的措辞）；删多余预览两条 key；两条创作类定稿「所行之处，皆归于此。」「还没有旅途。山海未远，等你启程。」 |
| 5 | auth | 31 | 已改 | `a8106ab` | 删 `resetOk`（与 `passwordChanged` 同文案）；`invalidEmail` 统一「请填写」；`failed` 改「操作失败，请重试」；`registering` 改「创建中…」 |
| 6 | settle | 21 | 已改 | `55a2870` `e05dbfd` `4a8e09b` `0eba8aa` | 删 8 条走查页 key（改明文）；常规版 7 条定稿（`title` 证书→识别结果，「证书」收进主题层）；`opening`/`backAlbum` 挪进可文案区；套册 8 条收回固定区（默认不入主题）；**日光图版五条已拍板**（含「定名图版」「此行所见，已录其名。」，待落机制）；清透＝老文案 + `opening` 一处美化、「title」建议不动（待点头）；**机制已落**：`VoiceId` 扩为 `"default" | "clear" | "daylight"`，覆盖表 `voices/clear.ts`（4 条：请过目 / 正在显影… / 收下 / lede）与 `voices/daylight.ts`（6 条：启封 / 启封中… / 入册 / 入册中… / 定名图版 / lede），`THEME_META` 各自指过去；`settle.claiming` 挪进可文案区（日光「入册中…」、清透沿用基础表）；相册格的 `aria-label` 曾拆出为 `album.openObservation`，**2026-09-15 随全仓删 `aria-label` 一并删除**——`settle.open` 回到单一 key，封条与结算页按钮都走主题说法；`check-voice.py` 加查 `voices/*.ts`（否则新表没人管）；`index.ts` 注释、`皮肤主题.md` §5、`品牌叙事.md:14` 一并改；清透那格原写「鉴定证书」——**撤回「不动」**：源码实测「专家」「鉴定」零命中，`f7ea283`「剥离专家人格、鉴定改识别」是项目级方向，`title` 两次提案均被否（「识别证书」拼得怪；「这张证书」拿未审的 `collection.lede` 当依据）→ **结论：`title` 不给主题覆盖，清透走基础表「识别结果」**，仪式感由已定的四条老文案承担；**旧词文档已清（本轮）**：`05-技术方案.md:148-149,97,104,117,251,254`、`OPS.md:648`、`皮肤主题.md:409,470`、`SPEC.md:93,369,375`、`品牌叙事.md:89`、`03-需求辨明.md:62`、wip 台账与演示页，共 14 处；`docs/brand/preview.html`、`flow.html` 是旧案对照页，已把「现行」标成「旧案（已废）」保留（要不要整页删另议） |
| 7 | volume | 30 | 跳过 | — | 用户明示「套册不管」，内容数据非界面文案 |
| 8 | error | 24 | 已改 | `cdbfa40` | 删 5：`identifyTooCoarse`（与 `status.tooCoarse` 同句）、`invalidPath`（改指 `notFound`）、3 条资格判定兜底（上游必填，同 detail 那次）；改 3：`identifyQuota` 额度→次数、`identifyUserKeyIncomplete` 字段名对齐「我的」页、`androidReleaseMissing` 与 `me.updateMissing` 统一 |
| 9 | collection | 25 | 已改 | `444e31c` `46be212` `a945d32` | 删 `volumeOpen`/`stampLift`（随删 aria-label）；物种卡「历次遇见」为空时**整节不渲染**（空态句删掉——既说不出「数据有误」也不是真指路；且正常流程不产生该状态）；`volumesEmpty`/`speciesNoMatch` 去句号；删 `petsCount` 并入 `speciesCount`；④ `lede`：基础表 B1「所见所识，皆录于此。」与日光 R1「所录之图版，皆入此册。」**已落**；清透五稿全废（形状都是「都在这里」式平铺说明句，撞创作类第 9 条的反例）；**曾建议「沿用老文案」——已撤回**（老文案是用户判过「一坨屎」的，属「未被采用的稿子」+「恢复原版不是处置」两条前车之鉴，等于抗命）；按规则重写三稿：① 「所见皆显，各有其名。」判「好点了但不满意」；② 「一路所见，皆已显影。」判「还在拼字 + 还是说明句」——用户指出我一直在**路/见/名/照/显/收**这几个字里排列组合，且 lede 不是说明文案、不需要告诉用户这页是干嘛用的；据此用户定下新守则，已落《文案撰写规范》创作类第 10 条「**创作类要发散，不是说明**」；③ 「它们都认得你了。」也被否（用户两问：**说了什么内容？美感又在哪儿？**——拟人是空话，且无意象无节奏）；④ 「一个都没丢。」也被否（0 分，无内容）；⑤ 「**有的是老朋友，有的是新相识。**」——按用户指令**上网查参照**后重写（博物馆前言/结束语：中国电影博物馆「慢慢地看吧。或久别重逢，或一见如故。」、湖南省博物馆「你既是唯一，也是万象」等，结构＝不描述展厅有什么、说你和这些东西的关系，邀请 + 并列两种关系 + 对句）——**已落** `voices/clear.ts`（无句号）；⑥ 宠物卡「历次遇见」为空时**整节连标题不渲染**（原错用 `collection.petsEmpty`，在宠物卡里成了假话——这条宠物明明已收录；与物种卡同处置，`petsEmpty` 保留给宠物列表页）；⑦ 三条 lede 去句号对齐（基础表「所见所识，皆录于此」、日光「所录之图版，皆入此册」） |
| 10 | tree3d | 26 | 已改 | `e6b096d` | 删 `tree3d.close`（随全仓删 aria-label）、`gotCountGo` 并入 `gotCount`（箭头交按钮渲染）；**调性＝书面语**（用户定）：`noChildren`「这一支尚未细分」/ `notCollectedSpecies`「尚未拍到过」/ `notCollectedBranch`「这一支尚未去过」/ `budNext`「余 {count} 个」/ `backToTree`「← 返回树」（箭头不动）——只换语气不换事实；核过不改：`budNext`/`budPrev` 的量词「个」（`batchTotal` 是这一扇的子级数 / 焦点自身的种数，非收录物）；演示页 `tree3d-review.html` 随模块入库 |
| 11 | album | 25 | 已改 | `2eb23e7` | 删 `album.liftPhoto`/`album.openObservation`（随全仓删 aria-label）；**去句号 11 条**（纪律 1；同族 `pickFailed` 本就无句号，此前一节里两种写法）：`cameraQualityHint`/`tooManyPhotos`/`uploadPartial`/`uploadPartialDup`/`uploadPartialMixed`/`uploadAllFailed`/`duplicatePhoto`/`fileTooLarge`（服务端 400 返回）/`pendingHint`/`readyToast`；`uploadAllDuplicates` 同时去尾句「没有新照片加入」（同义反复）；句号保留 2 条：`deleteConfirm`（问 + 连带影响）、`empty`（陈述 + 出路）；该删 0、该合并 0（`uploadPartial` 三条报三件不同的事；`uploading` 与 `uploadingProgress` 两态）；演示页 `album-review.html` 随模块入库 |
| 12 | share | 25 | 已改 | `e97954a` | 该删 0、该合并 0（`copyCode`/`copied`、`kick`/`kicking`、`leave`/`leaving` 三对是「动作/进行中」两态；`actionFailed` 与 `auth.failed` 同句也不合并——规范 §二 key 必须独立）；改 2：`actionFailed`「操作失败」→「操作失败，请重试」（同族都给一条出路）、`allowJoinHint` 去句号 + 「进不来」→「无法进入」；句号保留 2：`leaveConfirm`、`dissolveHint`；**9 条是服务端错误**（`apps/api/src/routes/trips.ts:134-142` 的 code→key 映射）；`share.you` 三处共用（管理旅途 / 详情 / 相册）；演示页 `share-review.html` 随模块入库 |
| 13 | rank | 17 | 已改 | `d54ef33` | 前判免检（国标译名），**到步核出 1 条真问题**：`rank.division`「部」→「**门**」——上网核到 division 是植物/真菌/藻类里相当于「门」的阶元（动物学才用 phylum），渲染点 `formatRank()`（`packages/messages/src/index.ts:93`）→ `TreeScene.ts:2707` / `SpeciesTree3D.tsx:234` / `TripAlbumPage.tsx:737` / `ObservationDetailPage.tsx:387` / `ObservationSettlePage.tsx:247`；其余 16 条为通行译名，与别名表 17 个中文直出完全一致。**核的来源如实记**：百度百科与维基「分类阶元」两条页面都 403 打不开，只有搜索摘要覆盖七级基本阶元，更细的 8 条（总科/亚科/族/下目/亚目/总纲/亚纲/域）未逐条核到权威源 |
| 14 | common | 15 | 已审·无改动 | — | 15 条通用词逐条核完无可动项：无重复句（`password`/`newPassword` 是两个字段；`confirm`/`confirmTitle` 一个是按钮一个是标题）、无句号问题（全无句号，`loadFailed`/`deleteFailed` 是各页 catch 的最短兜底）、无分型冗余（`saving`/`deleting`、`login`/`loggingIn`、`saved` 是不同状态）；一处写法不一致（`common.back` 的箭头在 JSX、`tree3d.backToTree` 的箭头在文案）按用户意见不动 |
| 15 | download | 9 | 已改 | `d2f93aa` | 删 step1；6 条去句号；iOS 去重复链接 |
| 16 | listTag | 10 | 已改 | `d49bd22` | 前判免检（法规定义），**到步核出 1 条过时表述**：`listTag.sanyou.hint` 原是 1988 年旧法的话（「有益的或者有重要经济、科学研究价值」），改为现行法定表述「**有重要生态、科学、社会价值的陆生野生动物**」（依据《野生动物保护法》第十条 + 国家林草局 2023 年第 17 号公告名录名；来源 gov.cn / forestry.gov.cn / 百度百科）；`listTag.domesticated`「驯养物种」→「**家养型物种**」（与自己说明里的词统一；后台「驯化位」属 admin、不动）；`extinct`「已灭绝」与说明「野外已难见到」强弱不一致，**用户定保持不变**；该删 0、该合并 0 |
| 17 | rarity | 7 | 已审·无改动 | — | 前判免检（档位锚点），到步核完无可动项：文案 7 档与服务端 `RARITY_TIERS`（`apps/api/src/rarity/scale-rubric.ts:31`「档位全序，越靠前越稀有，全仓库只认这一张表」）一一对应；说明成套递进（常见→少见→稀有→非常稀有→极稀有→传说→近乎不可能）；与名录门一致（灭绝名录直接锁 XR，`:277-279`）；全无句号；`LR`「传说」与其余档的「频率维度」不齐，用户定不动 |
| 18 | map | 6 | 已审·无改动 | — | 删 `map.openDetail`（随全仓删 aria-label）；逐条核完无可动项：两条署名是法务（`tiandituAttribution` 带审图号 GS(2025)1508号、`simpleBasemapAttribution`「Natural Earth」底图许可），不能改字；`map.empty` 是「现状 + 出路」两段独立信息（句号合规）；其余三条短句无句号；点位名兜底「观察」与条目名兜底「未命名」语境不同，用户定不统一 |
| 19 | onboard | 6 | 待审 | | |
| 20 | status | 6 | 待审 | | |
| 21 | identify | 6 | 待审 | | |
| 22 | app | 3 | 已改 | `7f565ab` | 品牌句定为「识尽众生，山海入怀」；`app.tagline`/`app.lede` 合并为一个 key |
| 23 | theme | 4 | 已改 | `9fb3387` | 2 条说明去句号，删与皮肤名重复的半句 |
| 24 | nav | 4 | 已审·无改动 | — | 四个导航锚点词（成套组），无可动项 |

## 维护方式

每过完一个模块：更新该行「状态」与「提交」；有改动的，在「备注」写一句改了什么。
**审阅页只有一个：`prototypes/clear-tree/review.html`**（2026-09-15 用户定）。每轮只放**当前这一块**的逐条审阅，过完一块就换成下一块——用户刷新即可，不再逐模块新建页、也不用重新拷地址。此前逐模块的演示页（`*-review.html`、`settle-*`、`copy-index.html` 等 26 个）已按用户指示删除，git 历史里可查。**给用户看的地址一律用项目 dev server 的 `/proto` 路径**（`http://127.0.0.1:5173/proto/clear-tree/review.html`）——`apps/web/vite.config.ts:54` 把 `prototypes/` 挂在这里，这才是 CodeBuddy 内预览能打开的地址；另起静态服务器（如 `python -m http.server 8899`）会开到预览之外，用户看不到。

**提案呈现方式（2026-09-14 用户定）**：**保留的也要逐条展开**（`key · 文案 · 渲染点 · 判断依据`），用户要能审到每一条；改动照旧展开（现状 / 建议 / 理由）。
保留项**按类别分组**（锚点/导航词、技术字段名、状态/反馈、创作类…），用户认可这个分类方式，往后沿用。

**已落地的模块页要标「状态：已落（提交号）」**（2026-09-14 用户反馈）：改完并提交后，那一页就是**记录**，不再按「现状 / 建议」的待批格式重述；汇报时也不重列已落的改动。

**「自己想」= 自己写稿，不是自己定稿**（2026-09-14 用户澄清）：稿子由 agent 写（不再铺一堆候选让用户挑，也不推给别的模型），但**必须拍板后才动生产文件**（纪律 9）。未拍板就改并提交＝违规，要主动认、并给出回退路径。

**读屏名（`aria-label`）已全删（2026-09-15 用户定）**：来历——最早随 `dd06ca6`（早期脚手架）出现、**19 个提交**陆续在加，`docs/` 里没有一条要求它；用户判断「这不是我们的需求」。共删 19 处（14 个 tsx/ts 文件 + 6 个品牌 SVG + 2 个预览页），并删掉 **6 个只为它存在的 key**（`album.liftPhoto`、`album.openObservation`、`collection.volumeOpen`、`collection.stampLift`、`tree3d.close`、`map.openDetail`）；品牌 SVG 的生成器 `brand-make-mark.py` 也改了，免得下次导出又带回来。`settle.open` 因此回到单一 key（封条与结算页按钮都走主题说法）。验收：相册页逐像素 **0 差异**、死 key 闸 **0 幽灵引用**、`tsc -b` 通过。**其余 aria 家族已一并清掉（2026-09-15 同批）**：`aria-hidden`×66 属性、`aria-modal`×6、`aria-pressed`×4、`aria-labelledby`×2、`aria-selected`×1、`role`×14（`dialog`×6 / `presentation`×4 / `group`×2 / `tablist`×1 / `tab`×1；交接单提到的 `role="img"` 在这四个目录实测 0 处，只在未入库的 `prototypes/pets-page/window.html:505` 有一处，事后补扫已清）、`sr-only` 4 处用法 + 1 条 CSS 规则，`src`+`packages` 共 93 处；另按用户「两处都清」扫了 `apps/web/public/` 预览页 8 处与 `docs/wip/` 演示页 21 处，合计 **122 处**。`ClearRevealStage.css` 那句注释里的 `aria-hidden` 字样一并改写（否则归零 grep 过不了）；`ObservationSettlePage` 因 `noUnusedLocals` 连带删掉只为 sr-only 播报存在的局部 `rarityLabel` 与 `type Rarity`/`type MessageKey` 导入。未动 `alt`、`inert`、`hidden`（有真实行为）。`prototypes/` 当时按交接单纪律未碰，但口径已变（`6a70fbc` 起演示页随模块入库，现 39 个文件在库）——事后补扫全仓，又清掉两处真实属性：`clear-tree/v2-roots.html:189` 的 `aria-label="关闭"`、未入库的 `pets-page/window.html:505` 的 `role="img" aria-label="窗子"`。至此**全仓真实 ARIA 属性 0 处**，剩余命中都是设计说明文字（如 `settle-open-places.html` 把 `aria-label="看看是什么"` 当代码展示那处）。另注：`model-bakeoff/run_bakeoff.py:268` 的 `role="user"` 是 LLM 消息角色、不是 ARIA，未动。验收：`tsc -b` EXIT=0、`pnpm check:copy` 通过、死 key 闸 0/0（737 key 未变）、归零 grep 在四个目录均无命中、`/` 与 `/me` 逐像素 **0 差异**（改动前先验了「同代码连截两次也是 0 差异」这条前提）。

## 纪律（沿用，用户立）

**文案**

1. 尽量不用句号（短句 / 状态 / 提示 / 按钮一律无句号；仅「多个各自独立、缺了看不懂的信息单元」才用句号）。
2. 禁同义反复。
3. 亲切短句优于完整陈述。
4. 说明性文案过「存在意义」三问：① 能据此行动吗 ② 成功时是废话吗 ③ 失败时已有另一条在说吗。皆否 → 删。
5. 事实正确 ≠ 需要告诉用户。属实只是「可以写」的必要条件。
6. 与界面已有元素重复的文字删。
7. 提案至少覆盖「该删 / 该合并 / 该改措辞」三类，只改措辞等于没审。
8. 判据用于淘汰，不用于生成。

**工程**

9. 动手前先复述计划、获确认再执行；未确认不写生产文件。
10. 只提交推送自己这轮动的文件。
11. 删 key 连带清代码引用（全局搜），否则 `t()` 回退显示 key 名。
12. 只删显示层，防重入锁、状态机等逻辑别误删。
13. 事实断言必须带可复现命令与输出。
14. 回答收在用户问的那一句上。
15. 现状靠读源码推导，不靠注释 / 文档结论 / 提交说明 / 截图。
16. 提交说明用中文。
17. 代码注释引正式文档，禁 `docs/wip/` 路径。

## 前车之鉴（避开）

- **没查清来源就凭字面动刀**：误把已定稿的 `error.identifySoftEncounter` 当旧文案要改。
- **统计必须覆盖多段 key**：只认两段式的正则会漏掉多段 key（760 曾漏成 417）。
- **模块状态一律 `git log` 实算**，不凭记忆说某模块做没做。
- **死 key 判定要覆盖 `packages/` 映射表**：`formatRank()` 的映射不在前端。
- **改前查清一条 key 被哪些码共用**，否则会写出「拍到人时是假话」的方案。
- **「看看」「演示」= 出展示页、零改动**，不等于批准改代码。
- **未被采用的稿子就是被否决**，不要重新端出来当候选。
- **「恢复原版」≠ 处置**：有代价的候选必须改到能打，否则等于丢。
- **别拿关键词拼贴当写作**：从品牌语料抠「遇见 / 名字 / 图鉴」排列组合，是蹩脚造句，不是写作。
- **文案的事实底线是「不虚假宣传」，不是「覆盖所有失败分支」**；lede 效果第一，修辞放大是本职，不是撒谎。
- **把品牌锚当不能动的框**：把「路上遇见的」当成必须保留的锚，新意被锁死；还把文档里的「路边随手拍」当成限制用户想象的框。品牌句该给用户**更大的自我**，不是缩小它。
- **覆盖写文件前必须先读**：整篇 `write` 覆盖会丢掉没看到的整节——`文案撰写规范.md` 曾被覆盖掉「术语口径」「不做无意义兜底」两节与「规则越少越好」立规原则，靠 `git diff` 才捞回来。改造 = 增删改，不是重写。
- **动一条 key 前先 `git log -S "那句话"` 查来历**：`detail.softReasonFallback` 是 `605195b` 特意把 4 条分型 key 合并后留下的（那次选「只留一种」），字面看不出这层来历；不查就动，等于重开一个已决问题。
- **别拿未审模块的文案当依据**：`collection` 还是待审模块，它的文案就是待改的候选；拿它给新词做立足点，等于把烂文案扶成基准。造句前先确认依据**已审 / 已定**。
- **按各层动词填空不是创作**：给三层各配一个「自己的动词」（收录 / 收下 / 入册）再套「X 的都在这里」，得到的是小学生句。创作要**接着该层已定稿的那句话往下说**——每层都有一句定稿的 settle lede，那才是这层真正的声音。（2026-09-15 用户第二次提醒「你知道这属于创作对吧」。）**写 lede 前先把项目里已定稿的 lede 捞出来看语法**（`grep '"lede"' zh.ts`）：8–16 字、一句话、必有一个具体动作或物件、口语或文言**成套**（同一层不混档）、「你」只嵌在动作里、从不做主语。
- **别把界面动作说成事物的本质**：「起个名字」不等于「旅途」本身。写「还没有旅途。起个名字，就有了。」被用户判 0 分——旅途是真的出了那趟门，起名只是创建流程里的一步。同理「换个底色」不等于「换个地方看照片」。（已入《文案撰写规范》创作类第 8 条。）
- **走查页 / 开发页的文案不落术语表**：路由挂 `/dev/*` 或只供本地走查的页面，文案直接写明文。`SettleArtPreviewPage` 的 8 条 `settle.preview.*` 曾混在用户术语表里——会被计入改造工作量，也可能被皮肤 voice 误覆盖。判据是「用户走得到吗」。（已入《文案工程规范》§一 例外。）
- **主题文本的 key 必须独立**：可文案区的 key 不得与 `common.*` 或其它共用 key 合并——voice 覆盖按 key 生效，合并即失去覆盖粒度（`common.*` 全在固定区，并进去就永远覆盖不了）。即使两套主题当下与 common 同句也不合并（如 `settle.failed` 与 `auth.failed`）。（已入《文案工程规范》§二 例外。）
