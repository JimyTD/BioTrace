# 0.2 施工备注（临时）

> **性质：施工备注，不是功能真源。**  
> 查「某能力做没做 / 正式规格」仍以 [`SPEC.md`](../SPEC.md) 为准；专题进 [`features/`](../features/)；运维进 [`OPS.md`](../OPS.md)。  
> 本文件只服务 0.2 分段开发与讨论；**一段收口后，把结论写回正式文档，再删或清空对应段落**。勿把此处当长期手册。  
> 创建：2026-08-11

## 0. 分工（当前共识）

| 线 | 谁 | 备注 |
|----|-----|------|
| 识图护栏（日额度 + 自备 Key） | **本线主责** | 下节起一段段做 |
| 轻社交 | **本线主责** | 护栏之后再开；第一刀形态未定 |
| 美术大更新 | 设计师 agent | 并线；0.2 内容之一 |
| 皮肤 `tide` +「我的」切换 UI | 设计师 agent | 并线 |
| 分发下一档（HTTPS / 域名等） | 配套，非本线主功能点 | 需要时跟 OPS |
| 0.1 遗留五条 | 其他 agent 排查 | 本线不管 |

原则：**贪多嚼不烂**——同时只推进一段；下一段开聊前，上一段至少有可验收结论（或明确搁置）。

## 1. 已收口：识图护栏（代码已推 `186df76` · SPEC/features 尚未写回）

### 1.1 来源（原则，非实现清单）

[`planning/05-技术方案.md`](../planning/05-技术方案.md) A.5：

- 平台默认 Key 在后端；默认 Gemini，备选智谱  
- 「我的」可存用户自备 Provider + Key；此后该用户识图走自备 Key  
- 前端永不打包平台 Key；用户 Key 入库、不完整回显  
- 每账号每日**默认**识图次数上限（N 未定）；超限提示自备 Key 或次日  
- 可加 IP / 全局限流（非本段必做）

现状（代码粗看）：有供应商侧 `identify_quota` / Gemini `daily_exhausted` 回退 GLM；**没有**按账号的日额度计数，也没有用户自备 Key 存储与路由。`SPEC` §5「我的」仍写自备 Key / 日额度后置。

### 1.2 本段目标（草案，待讨论锁死）

先做能挡住「注册即可刷爆平台 Key」的最小闭环：

1. **账号日额度**：仅消耗**平台默认 Key**的识图计入；超限拒绝新排队/新识图，稳定 error code + messages 文案。  
2. **自备 Key**：在「我的」写入/清除；有自备则该用户识图走自备，**不占**平台日额度（失败策略另定）。  
3. **不在本段**：IP/全局限流、未登录逛壳耗额度、多 Provider 花活、账单级用量看板。

### 1.3 已拍板 / 待讨论

| # | 题 | 状态 | 结论 |
|---|-----|------|------|
| Q1 | 默认日额度 N | ✅ | **100 / UTC 日 / 账号**；env `IDENTIFY_DAILY_LIMIT` 可改；`0` = 关闭账号日限额 |
| Q2 | 什么算一次 | ✅ | 每次实际调用**平台**识图扣 1（含重识别）；`failed` / 不可收集也扣；排队未跑不扣；自备 Key 全程不扣平台额度 |
| Q3 | 日界 | ✅ | **UTC 日** |
| Q4 | 超限体验 | ✅ | **松**：超限仍可上传进相册，**不跑识图**（失败态 + 文案引导填自备 Key / 等 UTC 次日）；重识别同理拦截。不把注定失败的活推进队列 |
| Q5 | 自备接哪些家 | ✅ | **方案 A**：仅 OpenAI 兼容口（Base URL + API Key + Model）；不设 Gemini/智谱用户预设 |
| Q6 | 自备失败 / 开关 | ✅ | **显式开关**「使用我的 Key」；开着则自备失败**硬失败**、不回落平台；关上 → 平台默认（计日额度）；Key 可留着只关开关 |
| Q7 | Key 存储与回显 | ✅ | 服务端加密；API 只回已配置标记 + 后四位等 hint；可清除 |

#### 1.3.1 自备 Key（已定摘要）

- **A**：用户自备只走 **OpenAI 兼容** 一条适配（Base URL + Key + Model 均必填）。  
- 平台默认仍是后端 Gemini → 智谱回退（我们自用免费效果好）；与用户自备通道分离。  
- 开关打开 → 只用用户兼容口，不掺平台 Key / 回退链。  
- 不做：厂商预设下拉、模型列表探测。  
- 「我的」：开关 + 三字段表单 + 今日平台 `已用/100`（仅走平台时计数）。  

**本段仍不做：** IP/全局限流、用量看板、多 Key 负载均衡。

#### 1.3.2 本段已全部拍板

| 项 | 结论 |
|----|------|
| 日额度 | 100 / UTC 日 / 账号；`IDENTIFY_DAILY_LIMIT`；`0`=关 |
| 扣次 | 平台识图实际调用才扣（含重识别、失败/不可收集）；自备不扣 |
| 超限 | 可上传不识图；引导自备 Key 或次日 |
| 自备 | OpenAI 兼容三件套 + 显式开关；开=硬失败不回落；关=平台 |
| 存储 | 服务端加密；回显 hint 后四位 |

#### 1.3.3 实现进度

| 切片 | 状态 |
|------|------|
| 日额度计数 + 超限可存图不入队 +「我的」展示 | ✅ 已落地（未写回 SPEC） |
| 自备 Key（OpenAI 兼容 + 开关） | ✅ 已落地（未写回 SPEC） |
| 假识图 `IDENTIFY_MOCK=1` + `identify:guard-smoke` | ✅ 不烧云额度测护栏 |

### 1.4 待写回正式文档（护栏）

- [`SPEC.md`](../SPEC.md)：去掉「日额度/自备 Key 后置」，补行为与 env  
- 可选 `features/识图护栏.md`  
- **不要**把长规格永远留在本 wip 文件

## 2. 当前段：共享旅途相册（实现中 · 第一社交需求）

### 2.0 实现进度

| 切片 | 状态 |
|------|------|
| DB：inviteCode / allowJoin / trip_members / shared_collection_credits | ✅ |
| 服务：加入/退出/踢人/解散/顺位/补算/收回 | ✅ |
| API：相册成员权限、settle 代开、地图并入、files ACL | ✅ |
| 前端：加入码、管理共享、成员、仅删自己的、待开包代开 | ✅ 最小可用 |
| SPEC / features 写回 | ⬜ 收口后再写 |

朋友圈 / 广场 / Feed **后置**。本段只做「几人共同出行 → 一本共享相册」。

### 2.1 已拍 / 倾向

| # | 题 | 结论 |
|---|-----|------|
| T1 | 进组方式 | **共享码**（不做全局好友）；成员上限 **10** |
| T2 | 删除 | **只能删自己上传的**（主人也不能代删他人？→ 待一句确认） |
| T3 | 识图额度 | **谁传用谁的**；流程不变 |
| T4 | 共享什么 | **共享结果**（相册共看；图鉴/套册如何吃结果见 T5） |
| T5 | 图鉴/套册受益 | ✅ **甲**：共享旅途里任一成员开包成功 → **全体成员**图鉴/套册加点 |
| T6 | 码 | ✅ 每旅途**生成一次唯一码**（不换码）；管理员开关 **是否允许加入**；关闭后持码也进不来；**满 10** 同样进不来 |
| T7 | 管理员 | ✅ 创建者初始为管理员；可踢人 / 开关节流（允许加入）/ 解散；退出则**入团最早**者接任；**不可**代删他人观察；**仅管理员**可查看/分享邀请码 |
| T8 | 中途入伙 | ✅ **补算**：加入时对旅途内已开包观察做全额图鉴/套册同步 |
| T9 | 离团/被踢 | ✅ **全收回**：清掉这趟共享带来的图鉴 + 套册（两者绑定）；不拿别人同种图换封面 |
| T10 | 待开包 | ✅ **5b**：团员可见他人待开包；**任意成员可点开包**（仪式；数据已在上传者识图完成时定好）；照片归属仍为上传者 |
| T11 | 重识别 | ✅ 广播给全体成员按同一观察最新结果重算 |
| T12 | 地图 | ✅ 共享旅途显示全员落点 |
| T13 | 收回粒度 | ✅ 只清这趟共享贡献；自己另有同种开包则保留 |
| T14 | 解散 | ✅ 全员收回共享进度；**各自**建一同名**私有旅途**承接自己上传的观察（有图才建）；不删他人文件；旅途名可重复（非凭证） |

### 2.2 受益模型（已定）

- 相册共看；上传者差异保留（额度、删除、文件一份在 `uploads/{obsId}/`）。  
- 开包成功 → 当前全体成员图鉴/套册加点（封面指针可指向他人观察，**仅在队期间**）。  
- 入伙 → 全额补算历史已开包。  
- 离团/被踢 → **全额收回**该旅途贡献的图鉴条目与套册亮槽；不再依赖断指针修复。  
- 在队时上传者删自己的图：按现有单人逻辑修**仍在队**成员的进度（可掉条目/灭槽）；**禁止**用第三人同种照片顶封面。

### 2.3 本段拍板摘要（可拆表开工）

有管理员（创建者；退出顺位继承）· 唯一邀请码 +「允许加入」开关 · 仅管理员踢人/开关/解散/看码 · 上限 10 · 谁传谁额度 · 只能删自己的 · 开包全员加点 · 任意成员可点开包仪式 · 入伙全额补算 · 离团全额收回图鉴+套册 · 地图全员落点 · 重识别广播 · 不做好友/Feed。

## 3. 明确不进本线主功能点

- 美术大更新、皮肤切换（设计师）  
- HTTPS / 备案域名 / 上架（分发配套）  
- Out 项：强制社区鉴定、AR 实时识别、录轨、地图连线等（见 `planning/03`）
