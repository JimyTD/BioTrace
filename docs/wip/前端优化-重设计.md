# 前端优化 · 重设计（临时）

> **性质：施工计划，不是功能真源。**  
> 功能做没做仍以 [`SPEC.md`](../SPEC.md) 为准；皮肤/套册分层以 [`features/皮肤主题.md`](../features/皮肤主题.md)、[`features/套册美术分层.md`](../features/套册美术分层.md) 为准。  
> 本文件只服务「整站前端翻新」这一条线。一段收口后，把**已定视觉与交互原则**写回 `features/`（或 SPEC 表现小节），再删或大幅缩本文件。  
> 创建：2026-08-13

## 0. 这句话先钉死

产品已经有主路径（旅途 → 相册上传 → 开包高潮 → 图鉴/套册），缺的不是新功能堆砌，是**整站还不像同一本旅行相册**。

- **开包**已接近高潮完成度（纸、框、火漆）。  
- **旅途列表 / 相册格 /「我的」信息架构**刚开了头，仍粗糙。  
- **登录、地图、图鉴、观察详情、相册管理/共享**基本还是通用表单 + 圆角卡片。

翻新范围：**交互逻辑、排版层级、主题色调、美术资源**。不改识图/稀有度结算核心。

### 0.1 三层：大体不动，图可换

从第一刀就要按这个拆，不要「先出一张很好看的图再倒逼布局」。

```text
1. 骨架     这一屏有什么、谁主谁次、点哪里进二级     ← 大体
2. Token    色 / 字 / 圆角 / 阴影                       ← 换皮
3. 槽位资源  约定文件名、比例、是否透明、内窗 inset      ← 可替换
```

| 层 | 改它等于 | 不该发生的事 |
|----|----------|----------------|
| 骨架 | 改产品怎么用 | 为换一张底图复制页面、改路由、改开包流程 |
| Token | 整站换气质 | 只在某一页写死 hex |
| 槽位 | 换一张 PNG/SVG | 组件写死 `/trips/daylight/foo.png`；图自带相框/文字；半透明边垫当框 |

**槽位要先写契约再出图。** 例如「底栏」契约可以是：高度固定、四项、激活态用 token；若以后要纸纹，只加 `nav-texture.png` 铺在栏内，栏的结构和点击区域不变。封面框契约已经是：几何 SVG、中心挖空、边垫不透明、照片在 6% 内窗。换一张 `cover-frame.svg` 不应改 `TripsPage` 逻辑。

Cut A 问「底栏要不要邮戳感」本质是：**骨架先做成标准底栏**；邮戳/纸纹是可选槽位，没有图时 token 底也能用。先花活再骨架，图一换整页就塌。

出图纪律仍见 §5；helper 只经 `themes/*Assets.ts`。

---

## 1. 已验证的原则（本线必须遵守）

从开包、旅途、我的三轮实战里收来的，不要再踩：

| # | 原则 | 反面教材 |
|---|------|----------|
| P1 | **主屏变薄。** 日常路径留在第一屏；改密、高级配置、运维状态进二级 | 「我的」曾把 UUID、Gemini 状态、改密表单全摊开 |
| P2 | **照片优先，框是几何 SVG。** 边垫必须不透明；照片放进 overflow 内窗再缩放 | 半透明框 + hover scale → 穿模 |
| P3 | **空态用文案 + 纸色，不要整幅「摆拍风景」** | 旅途空态插画风格互掐、带字、难溶进页面 |
| P4 | **本地静态预览要像真机结构**（顶栏 + 内容 + 底栏），再用用户照片样片，不用抽象占位 | 绿椭圆「测框图」进预览 |
| P5 | **文案直白。** 按钮写「创建」不写「开册」；lede 给人看，不写「环境状态」 | — |
| P6 | 表现三分离：文案 `packages/messages` · token `themes/*.css` · 结构 `styles.css` + `pages/` | 禁止页内写死 hex / `/volumes/daylight/` |
| P7 | 换皮只换 token + `public/<pack>/<theme>/`，**不复制页面** | `tide` 仍后置，本线先把 daylight 做完整 |
| P8 | 满意再推；真机 WebView 不信 CSS mask | 火漆方底、框线错位 |
| P9 | **先骨架与槽位契约，后出图。** 美术可换，不能倒逼布局 | 空态风景图风格不统一、带字、难替换 |

气质锚点（日光胶片）：**暖纸底、柏褐字、陶橙 CTA、铜版/齿孔几何、开包才用火漆高潮。** 不是 scrapbook 摆拍，不是紫霓虹抽卡，不是冷绿 SaaS。

---

## 2. 现状盘点（按表面）

| 表面 | 完成度 | 主要问题 |
|------|--------|----------|
| 开包 `SettlePackStage` | 高 | 高潮已立；细节可再磨（动效、真机） |
| 旅途列表 | 中 | 封面墙+框刚修穿模；创建条、共享角标仍工具感 |
| 旅途相册 | 中 | Cut B：第一屏格子；管理进二级页；选图后才展开上传器 |
| 我的 | 中 | 主屏变薄已推；二级页还是素表单；皮肤切换未做 |
| 图鉴/套册 | 低 | 有册皮资源，列表仍像目录+方格；弹层仪式未对齐开包完成度 |
| 地图 / 钉点 | 低 | 功能图；选中卡、空态、控件与纸感脱节 |
| 观察详情 | 低 | 长表单栈：图、分类、操作、重识别挤在一起 |
| 登录 | 低 | `brand` + 三模式表单，没有「进相册」的第一印象 |
| 壳（顶栏/底栏） | 高 | Cut A 已推：视口锁壳、页头契约、纸纹槽可空 |
| 皮肤 `tide` | 未做 | 文档后置；本线不插队，除非 daylight 主路径已像样 |

---

## 3. 交互逻辑：要改的不是「加功能」

### 3.1 全站共性

- **第一屏只回答一件事。** 旅途=封面墙；相册=看图+加入；图鉴=册与种；地图=点；我的=这是谁。  
- **设置与管理用二级页或明确「管理」开关**，不要默认展开表单。相册「管理旅途」已进 `/trips/:id/manage`。  
- **主按钮每屏最多一个**（加入照片 / 请过目 / 创建）。其余 `secondary` 或行+箭头。  
- **错误与空态说人话**，技术细节（UTC、error code、用户 ID）放二级或日志。  
- **共享旅途**控件（邀请码、成员、踢人）是管理员路径，默认藏进「管理」。

### 3.2 分表面要点

| 表面 | 交互方向 |
|------|----------|
| 登录 | 默认只露登录；注册/重置是切卡，不是三个表单叠在一起抢视线 |
| 旅途列表 | 保持内联创建但视觉降权；共享旅途用角标，不在封面下堆三行 meta |
| 相册 | 默认：头（返回/标题/摘要）+ 加入 + 胶片格。上传器点开才展开。管理/共享进二级或底部抽屉，勿与格子抢第一屏 |
| 开包 | 维持封缄→揭示→展出；勿把套册仪式塞进开包主路径 |
| 图鉴 | 上：套册横滑（实体册皮）。下：物种格。点册开弹层，不要和物种列表抢同一视觉重量 |
| 地图 | 底图为主；选中用底部信息条（图+名+进相册），少用地图上浮大卡 |
| 详情 | 上大图；中名字/稀有度/一句简介；下「分类」折叠；危险操作（删、重识别）沉底 |
| 我的 | 已按此做。后续只补皮肤入口，勿把表单搬回主屏 |

---

## 4. 排版与主题色调

### 4.1 结构统一（`styles.css`）

所有 Tab 用同一套页头：`.page-head` + `.page-title` + 可选 `.lede`。禁止再混用登录页 `.brand` 当内页标题。

抽可复用件（不要每个页一套）：

- `.me-menu` / `.me-row` → 升为通用 **设置行**（账号安全、图鉴筛选项等）  
- `.me-identity` → 通用 **名片/扉页条**  
- `.me-section` → 通用 **二级页卡片**  
- 胶片格 / 封面窗：已有 `.film-tile-window`、`.trip-cover-window`，图鉴与详情 hero 向它对齐

内联 `style={{ gap: "0.35rem" }}` 收进 `.field` / `.me-field`。

### 4.2 Token（`daylight.css`）

先把 daylight **用全、用对**，再谈第二套皮肤：

- 清理 `styles.css` 残留硬编码 hex（如部分 backdrop）  
- 补齐「设置行 / 名片 / 二级卡片」语义 token，与 `--trip-*`、`--volume-*` 并列  
- 底栏、顶栏与页面氛围同一纸色家族，避免一块灰一块米  
- 稀有度色阶已定（灰→蓝→紫→金→红→彩虹→洋红），徽章与火漆继续共用，不要图鉴再发明一套

`tide`：本线后段；checklist 仍见皮肤主题手册 §3。本文件不展开潜水气质。

### 4.3 动效

克制：内窗内轻 scale、开包壳抬起、空态淡入。禁止封面整体抬升导致穿底栏。新动效先在静态预览里看，再进 React。

---

## 5. 美术资源

| 包 | 路径 | 本线态度 |
|----|------|----------|
| 开包 | `public/settle/<theme>/` | 维护；框/章走几何或内联 SVG，勿回 CSS mask |
| 套册 | `public/volumes/<theme>/` | 册皮可用；弹层/空印座按分层文档继续几何框 |
| 旅途 | `public/trips/<theme>/` | **只留框 SVG**；空态氛围 JPG 已证明失败，不要再加 |
| 登录/地图/我的 | 无专用包 | 优先 CSS 纸感；缺了再出**单职责**资源（勿生活照桌面） |

出新图的纪律：

1. 与 `pack-bg` / 册皮同一铜版纸语汇  
2. 无字、无画框、中心可挖空则挖空  
3. 叠层透明、氛围底可整幅  
4. 先 `public/<pack>/preview.html` 真机结构验收，再接入页面

---

## 6. 施工顺序（建议一次只做一块表面）

## 6. 施工顺序（建议一次只做一块表面）

贪多嚼不烂。每块：**看现状 → 改交互/排版 → 必要时补资源 → 本地预览 → 真机 → 再下一块。**  
**不跳刀。** A 看起来「变化小」也要先做：后面每页都长在这副壳上。不为了「先看见大改」去抢 B。

| 序 | 块 | 目标（怎样算像样） | 依赖 |
|----|----|-------------------|------|
| A | 壳 | 见 §6.1；验收：四 Tab 页头同一套，底栏不再被内容穿过 | 无 |
| B | 相册页（旅途内） | 第一屏是格子；上传/管理/共享不抢戏 | A |
| C | 旅途列表收尾 | 创建条题签化、共享角标、meta 一行 | A；与 B 勿抢同一对话 |
| D | 图鉴 + 套册弹层 | 册皮墙 + 物种格层级清楚；弹层像册不是后台 | 套册资源已有 |
| E | 观察详情 | 大图叙事；分类折叠；操作沉底 | D 后更顺 |
| F | 地图 + 钉点 | 底图主角；选中条；空态一句话 | 功能已稳定 |
| G | 登录 | 进门像同一产品 | 壳完成后随时可做 |
| H | 我的收尾 | 「关于」已全端；皮肤切换轻入口 | `applyTheme` 已有 |
| I | 动效与真机债 | WebView、safe-area、穿模回归 | 各块做完扫一遍 |
| J | `tide` | 第二皮肤 | daylight 主路径像样之后 |

### 6.1 Cut A · 壳（已推）

骨架已按锁定壳落地：顶栏 / 主区 / 底栏三行；主区单独滚；页头 `.page-head` + `.page-title`。纸纹槽 `--nav-texture: none`。静态预览 `public/shell/preview.html`。

**刻意后置（整条线）：** 朋友圈/Feed、tide 整包插画、大改地图引擎、为美化分叉业务路由（「我的」二级页这种信息架构改动除外）。

### 6.2 Cut B · 相册页（施工中 · 待预览验收）

**骨架：**

```text
┌ 返回                          管理旅途 → /trips/:id/manage
│ 旅途标题
│ 一行：张数 · 日期 · 地点 · 共享（有才显示）
│ [加入照片]  拍照
│ 胶片格
```

- 「加入照片」直接打开相册/文件选择；选中后才出现缩略图与「上传并鉴定」。
- 「拍照」仍在，主屏是文字链，选图后仍可改选。
- 管理页保留改名、摘要、共享、成员、离开、删除旅途。不删功能。
- 格子「可靠到」与单张删除这刀不动。

静态预览：`public/trips/album-preview.html`。

---

## 7. 每块开工检查单

- [ ] 这一屏第一件事是什么？其余是否二级？  
- [ ] 文案是否已进 `zh.ts`？有无硬编码中文？  
- [ ] 色是否只来自 `var(--*)`？新 token 是否写入 `daylight.css`？  
- [ ] 相框是否几何 SVG、边垫不透明、照片在内窗裁切？  
- [ ] 有无静态预览（真顶栏/底栏）？用的是正经照片样片吗？  
- [ ] 是否动了识图/稀有度核心？（本线不应动）

---

## 8. 建议的对话方式

整站翻新跨度大，**按上表一块表面开一条对话**（或一块收口后再开下一块）。新对话开头带：

1. 读本文件 §1 原则 + 当前要做的那一行  
2. 日光胶片；文案走 messages；token 换皮  
3. 先预览再推

不要在同一长对话里从开包串到地图——上下文会稀释，旧方案容易串台。
