# Local override: 国内服务器用镜像加速版Dockerfile 构建 API。
# docker compose 会自动加载本文件（无需 -f 显式指定），仅覆盖 build.dockerfile 字段，
# 其余（端口、卷、env_file）沿用 docker-compose.yml。
# 若在境外机（如新加坡）部署且 deb.debian.org / npmjs 直连很快，可删除本文件用默认 Dockerfile。
services:
  api:
    build:
      dockerfile: deploy/Dockerfile.cn
